#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main() {
    int n,T,a;
    T=6;
    n=2;
    a=5;
    srand(T);
    time_t curtime;
    time(&curtime);
    if(n>5) {
        printf("Invalid number!");
    } else {
    printf("======= Lotto 659 ========\n");
    printf("%s", ctime(&curtime));
    for(int j=1; j<a+1; j++) {
        printf("[%d]: ", j);
        if(j<n+1) {
        for(int i=0; i<T; i++) {
            if(i < 5) {
                printf("%02d ", rand() % 69);
            } else {
                printf("%02d ", rand() % 10);
            }
        }
    } else {
    for(int i=0; i<T; i++) {
        printf("-- ");
    }
}
    printf("\n");
    }
    printf("======= csie@CGU =========\n");
}
    return 0;
}
